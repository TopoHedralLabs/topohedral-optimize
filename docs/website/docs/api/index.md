# API Documentation

The generated Rust API documentation is installed at this location when the
documentation site is deployed.

For local API documentation, run:

```console
cargo doc --no-deps --all-features --open
```
